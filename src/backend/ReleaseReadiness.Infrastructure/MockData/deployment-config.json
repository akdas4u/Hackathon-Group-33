{
  "_mock": "MOCK: Replace with a config management API (e.g. Azure App Configuration) in production",
  "requiredKeys": ["DATABASE_CONNECTION_STRING", "PAYMENTS_API_KEY", "FEATURE_FLAGS_ENDPOINT", "REDIS_CONNECTION_STRING"],
  "missingKeys": ["PAYMENTS_API_KEY"],
  "summary": {
    "scenarios": {
      "Blocked": {
        "status": "Fail",
        "riskLevel": "Critical",
        "findings": ["Missing external API configuration key"],
        "evidence": "Key 'PAYMENTS_API_KEY' absent from release config bundle",
        "remediation": "Add PAYMENTS_API_KEY to release config before proceeding"
      },
      "Conditional": {
        "status": "Pass",
        "riskLevel": "Medium",
        "findings": ["All required keys present; 1 key still using a non-production default"],
        "evidence": "All 4 required keys present; FEATURE_FLAGS_ENDPOINT points at the staging endpoint in the release config bundle",
        "remediation": "Replace the staging FEATURE_FLAGS_ENDPOINT value with the production endpoint before deploying"
      },
      "Clean": {
        "status": "Pass",
        "riskLevel": "Low",
        "findings": ["All required configuration keys present"],
        "evidence": "PAYMENTS_API_KEY and all other required keys present in release config bundle",
        "remediation": null
      }
    }
  }
}
